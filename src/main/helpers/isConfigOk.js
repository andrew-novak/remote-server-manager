export default (config) => {
  try {
    const { host, username, privateKey } = config.ssh;
    const { temporary } = config;
    const { config: configDir, static: staticDir } = config.
  } catch (err) {
    return false;
  }
};
